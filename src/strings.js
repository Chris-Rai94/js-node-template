const fullName = "Christopher Stokley";

const courseNum = "CIS 177";

const expectedGrade = 95;

const string =
	fullName +
	" is taking " +
	courseNum +
	" and expects to earn " +
	expectedGrade +
	"%";

const literal = `${fullName} is taking ${courseNum} and expects to earn ${expectedGrade}%`;

console.log(string);

console.log(literal);
